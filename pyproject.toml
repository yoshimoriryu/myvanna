[tool.poetry]
name = "myvanna"
version = "0.1.0"
description = "Creating vanna with FastAPI"
authors = ["Your Name <you@example.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.116.1"
uvicorn = "^0.35.0"
vanna = "^0.7.9"
sqlalchemy = "^2.0.43"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
